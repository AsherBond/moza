= bootstrap_page_header "Edit Playlist", "#{@playlist.name}" 
.row
	.span8.offset4
		.well
			= render 'form'
	.span4
		= link_to "Delete Playlist", playlist_path(@playlist), :method => :delete, :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) }, :class => 'btn btn-large btn-danger'
