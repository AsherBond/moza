%li{:class => "#{span}"}
  = link_to image_tag(gallery.cover.url(:large)), gallery, :class => "thumbnail", "data-original-title" => "#{gallery.name}", "data-content" => "<p>#{gallery.description}</p>", :rel => "popover"